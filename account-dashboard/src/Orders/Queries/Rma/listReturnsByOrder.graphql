query listReturnsByOrder($orderId: Int, $pageSize: Int, $currentPage: Int) {
    listReturnsByOrder(orderId: $orderId, pageSize: $pageSize, currentPage: $currentPage) {
        total_count
        returns_by_order {
            id
            increment_id
            ship_from
            date_requested
            status
        }
    }
}
